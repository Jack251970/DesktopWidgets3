<UserControl
    x:Class="DesktopWidgets3.Widget.Jack251970.Performance.Views.PerformanceSetting"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:DesktopWidgets3.Widget.Jack251970.Performance.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:DesktopWidgets3.Widget.Jack251970.Performance.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:PerformanceSettingViewModel}"
    mc:Ignorable="d">

    <StackPanel Padding="{StaticResource ScrollViewerContentPadding}" Spacing="{StaticResource SettingsCardSpacing}">
        <controls:SettingsCard Description="Use celsius instead of fahrenheit as the unit of the temprature" Header="Show celsius">
            <ToggleSwitch IsOn="{x:Bind ViewModel.UseCelsius, Mode=TwoWay}" />
        </controls:SettingsCard>
    </StackPanel>
</UserControl>
