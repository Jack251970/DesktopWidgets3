<UserControl
    x:Class="DesktopWidgets3.Widget.Jack251970.Network.Views.NetworkSetting"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:DesktopWidgets3.Widget.Jack251970.Network.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:DesktopWidgets3.Widget.Jack251970.Network.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:NetworkSettingViewModel}"
    mc:Ignorable="d">

    <StackPanel Padding="{StaticResource ScrollViewerContentPadding}" Spacing="{StaticResource SettingsCardSpacing}">
        <controls:SettingsCard Description="Use Bps instead of B/s as the unit of the network speed" Header="Show Bps">
            <ToggleSwitch IsOn="{x:Bind ViewModel.UseBps, Mode=TwoWay}" />
        </controls:SettingsCard>
    </StackPanel>
</UserControl>
