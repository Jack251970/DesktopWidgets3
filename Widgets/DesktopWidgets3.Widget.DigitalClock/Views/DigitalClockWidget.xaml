<UserControl
    x:Class="DesktopWidgets3.Widget.DigitalClock.Views.ClockWidget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:DesktopWidgets3.Widget.DigitalClock.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:DigitalClockViewModel}"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" Background="Transparent">
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Vertical">
            <TextBlock HorizontalAlignment="Center" Text="{x:Bind ViewModel.SystemTime, Mode=OneWay}" />
        </StackPanel>
    </Grid>
</UserControl>
