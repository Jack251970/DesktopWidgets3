<Page
    x:Class="DesktopWidgets3.Views.Pages.Widget.Settings.FolderViewSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:settings="using:DesktopWidgets3.ViewModels.Pages.Widget.Settings"
    xmlns:behaviors="using:DesktopWidgets3.Behaviors"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    d:DataContext="{d:DesignInstance Type=settings:FolderViewSettingsViewModel}"
    behaviors:NavigationViewHeaderBehavior.HeaderContext="FolderView"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <ScrollViewer 
            IsTabStop="False"
            UseSystemFocusVisuals="False"
            VerticalScrollBarVisibility="Hidden"
            VerticalScrollMode="Auto">
            <StackPanel Spacing="{StaticResource SettingsCardSpacing}">
                <labs:SettingsCard x:Uid="WidgetSettings_FolderPath_Card">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock 
                            Text="{x:Bind ViewModel.FolderPath, Mode=OneWay}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            IsTextSelectionEnabled="True"
                            TextWrapping="Wrap"
                            VerticalAlignment="Center"
                            Margin="0,0,8,0"/>
                        <Button
                            Style="{StaticResource SettingsCardButton}"
                            Command="{x:Bind ViewModel.SelectFolderPathCommand}">
                            <FontIcon FontSize="14" Glyph="&#xe8da;" />
                        </Button>
                    </StackPanel>
                </labs:SettingsCard>
                
                <labs:SettingsCard x:Uid="WidgetSettings_ShowIconOverlay_Card">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.ShowIconOverlay, Mode=TwoWay}" />
                </labs:SettingsCard>

                <labs:SettingsCard x:Uid="WidgetSettings_ShowHiddenFile_Card">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.ShowHiddenFile, Mode=TwoWay}" />
                </labs:SettingsCard>

                <labs:SettingsCard x:Uid="WidgetSettings_AllowNavigation_Card">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.AllowNavigation, Mode=TwoWay}" />
                </labs:SettingsCard>

                <labs:SettingsCard x:Uid="WidgetSettings_ShowExtension_Card">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.ShowExtension, Mode=TwoWay}" />
                </labs:SettingsCard>

                <labs:SettingsCard x:Uid="WidgetSettings_ShowThumbnail_Card">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.ShowThumbnail, Mode=TwoWay}" />
                </labs:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
