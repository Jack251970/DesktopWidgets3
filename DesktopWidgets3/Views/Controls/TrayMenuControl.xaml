<UserControl
    x:Class="DesktopWidgets3.Views.Controls.TrayMenuControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d">

    <UserControl.Resources>
        <XamlUICommand x:Key="ShowCommand" ExecuteRequested="ShowWindow" />
        <XamlUICommand x:Key="ExitCommand" ExecuteRequested="ExitApp" />
    </UserControl.Resources>

    <tb:TaskbarIcon 
        x:Name="TrayIcon"
        x:Uid="TrayMenu_TaskBarIcon"
        ContextMenuMode="SecondWindow"
        IconSource="/Assets/WindowIcon.ico"
        DoubleClickCommand="{StaticResource ShowCommand}"
        NoLeftClickDelay="True"
        MenuActivation="RightClick">
        <tb:TaskbarIcon.ContextFlyout>
            <MenuFlyout AreOpenCloseAnimationsEnabled="False">
                <MenuFlyoutItem
                    x:Uid="TrayMenu_MenuFlyoutItem_ShowWindow"
                    Command="{StaticResource ShowCommand}"/>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem
                    x:Uid="TrayMenu_MenuFlyoutItem_ExitApp"
                    Command="{StaticResource ExitCommand}"/>
            </MenuFlyout>
        </tb:TaskbarIcon.ContextFlyout>
    </tb:TaskbarIcon>
</UserControl>
