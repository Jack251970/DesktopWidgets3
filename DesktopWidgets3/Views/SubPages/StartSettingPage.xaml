<Page
    x:Class="DesktopWidgets3.Views.SubPages.StartSettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewmodels="using:DesktopWidgets3.ViewModels.SubPages" 
    d:DataContext="{d:DesignInstance Type=viewmodels:StartSettingViewModel}"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Button HorizontalAlignment="Center" VerticalAlignment="Center"
            Content="{x:Bind ViewModel.StartLockingContent, Mode=OneTime}"
            Command="{x:Bind ViewModel.StartSettingCommand, Mode=OneWay}"/>
    </Grid>
</Page>
